const nav=document.querySelector('.nav'),menu=document.querySelector('.menu');menu.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.nav nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));addEventListener('scroll',()=>nav.classList.toggle('scrolled',scrollY>30));
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.15});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const stats=document.querySelector('.stats');let counted=false;new IntersectionObserver(([entry])=>{if(entry.isIntersecting&&!counted){counted=true;document.querySelectorAll('[data-count]').forEach(el=>{let end=+el.dataset.count,start=0,step=()=>{start+=Math.ceil(end/55);el.textContent=Math.min(start,end);if(start<end)requestAnimationFrame(step)};step()})}},{threshold:.5}).observe(stats);
document.querySelector('#enquiry').addEventListener('submit',e=>{e.preventDefault();const d=new FormData(e.currentTarget),msg=`Hello THUNDER FITNESS, I want to know more about membership.\n\nName: ${d.get('name')}\nPhone: ${d.get('phone')}\nFitness Goal: ${d.get('goal')}\nMessage: ${d.get('message')}`;location.href='https://wa.me/919229588102?text='+encodeURIComponent(msg)});
